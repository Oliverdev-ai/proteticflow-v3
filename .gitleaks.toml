title = "ProteticFlow - Gitleaks Config"

[allowlist]
  description = "Allow test placeholders in .env.example and CI workflow"
  paths = [".env.example", ".github/workflows/ci.yml"]
  regexTarget = "line"
  regexes = ["password", "minioadmin", "ci-test-secret"]
