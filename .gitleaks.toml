title = "ProteticFlow — Gitleaks Config"

[allowlist]
  description = "Exemplos no .env.example são permitidos"
  paths = [".env.example"]
  regexTarget = "line"
  regexes = ["password", "minioadmin", "ci-test-secret"]